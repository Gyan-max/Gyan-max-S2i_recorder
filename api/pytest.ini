[pytest]
# The suite uses async fixtures, which pytest-asyncio only picks up in auto mode.
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning
