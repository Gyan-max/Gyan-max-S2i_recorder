import React from 'react'
import ReactDOM from 'react-dom/client'
import Phase3App from './Phase3App.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Phase3App />
  </React.StrictMode>,
)
